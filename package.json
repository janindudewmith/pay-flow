{
  "name": "pay-flow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npx vite build",
    "start": "cd backend && npm install && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "vite": "^6.0.5"
  }
}
